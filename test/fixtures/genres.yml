# == Schema Information
#
# Table name: genres
#
#  id         :integer          not null, primary key
#  name       :string(255)
#  slug       :string(255)
#  created_at :datetime
#  updated_at :datetime
#
# Indexes
#
#  index_genres_on_slug  (slug) UNIQUE
#

# Intentionally empty: the default scaffold rows are unused and were stale
# (duplicate slugs violated the unique index). Tests build genres explicitly.

# == Schema Information
#
# Table name: prices
#
#  id               :integer          not null, primary key
#  cached_artist    :string(255)
#  media_type       :string(255)
#  cached_label     :string(255)
#  detail           :string(255)
#  price_low        :integer
#  price_high       :integer
#  yearbegin        :integer
#  yearend          :integer
#  created_at       :datetime
#  updated_at       :datetime
#  footnote         :text
#  artist_id        :integer
#  label_id         :integer
#  record_format_id :integer
#  user_id          :integer
#

# Intentionally empty: the default scaffold rows are unused and were stale.
# Tests that need prices build them explicitly.

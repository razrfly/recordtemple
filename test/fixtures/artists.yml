# == Schema Information
#
# Table name: artists
#
#  id         :integer          not null, primary key
#  name       :string(255)
#  slug       :string(255)
#  created_at :datetime
#  updated_at :datetime
#
# Indexes
#
#  index_artists_on_name  (name) UNIQUE
#  index_artists_on_slug  (slug) UNIQUE
#

# Intentionally empty: the default scaffold rows are unused and were stale
# (referenced a removed `freebase` column). Tests build artists explicitly.

# == Schema Information
#
# Table name: record_types
#
#  id         :integer          not null, primary key
#  name       :string(255)
#  created_at :datetime
#  updated_at :datetime
#

# Intentionally empty: the default scaffold rows are unused and were stale.
# Tests that need record types build them explicitly.

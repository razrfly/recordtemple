# == Schema Information
#
# Table name: record_formats
#
#  id             :integer          not null, primary key
#  name           :string(255)
#  created_at     :datetime
#  updated_at     :datetime
#  record_type_id :integer
#

# Intentionally empty: the default scaffold rows are unused and were stale.
# Tests that need record formats build them explicitly.

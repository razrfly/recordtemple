# == Schema Information
#
# Table name: users
#
#  id         :integer          not null, primary key
#  email      :string(255)      default(""), not null
#  username   :string(255)
#  created_at :datetime
#  updated_at :datetime
#

# Intentionally empty: the default scaffold rows referenced long-removed Devise
# columns (the app uses passwordless auth now). Tests create users explicitly.

= simple_form_for [:admin, @record_type] do |f|
  .grid.simple
    .grid-title.no-border
      h4
        ' Record
        span.semi-bold> Type
        | Form
    .grid-body.no-border
      = f.input :name
      .form-actions
        = f.button :submit, :class => 'btn-primary'

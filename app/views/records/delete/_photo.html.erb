<div id="photo">
  <p>
  <% form.fields_for :photos, photo, :child_index => (photo.new_record? ? "index_to_replace_with_js" : nil) do |photo_form| %>    
    <%= photo_form.file_field :data %>
    <%= link_to_function "delete", "$('#photo').remove();" %><br/>
  <% end %>
  </p>
</div>
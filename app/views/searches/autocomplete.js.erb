[
<% for result in @search %>
{
	"value": "<%= result.searchable_id %>",
	"label": "<%= truncate(result.term) %>",
	"desc": "<%= result.searchable_type %>",
	"icon": "jquery_32x32.png"
}<%= "," unless result.searchable_id == @search.last.searchable_id %>
<% end %>
]